/** One place for how serious an alert kind is: the server's src/alert_level.ts, re-exported so every page and the API order alerts the same way. */
export { alertLevel, ALERT_LEVELS, LEVEL_RANK } from "../../src/alert_level";
export type { AlertLevel } from "../../src/alert_level";
